// import PropTypes from 'prop-types';


const Payment = () => {
    return (
        <div>

            <p>Payment now</p>
        </div>
    );
};

// Payment.propTypes = {
    
// };

export default Payment;